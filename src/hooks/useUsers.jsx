import useAxiosSecure from "./useAxiosSecure";

const useUsers = () => {
    const axiosSecure = useAxiosSecure();
    return (
        <div>
            
        </div>
    );
};

export default useUsers;